<div>
  <div class="footer">
    <div class="button">
      <img src="{{home-icon}}">
      <span>Home</span>
    </div>
    {{#if isAdmin}}
      <div class="button">
        <img src="{{people-icon}}">
        <span>People</span>
      </div>
      <div class="button">
        <img src="{{settings-icon}}">
        <span>Settings</span>
      </div>
    {{#else}}
      <div class="button">
        <img src="{{you-icon}}">
        <span>Your work</span>
      </div>
    {{/if}}
    {{#if isDebugMode}}
      <div class="button">
        <img src="{{debug-icon}}">
        <span>System</span>
      </div>
    {{/if}}
  </div>
</div>