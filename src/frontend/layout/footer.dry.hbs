<div>
  <div class="footer">
    <div tabindex="70" class="button home{{#if page.home}} active {{/if}}">
      <div class="icon">
        {{{icons.home}}}
      </div>
      <span>Home</span>
    </div>
    {{#if isAdmin}}
      <div tabindex="71" class="button admin{{#if page.people}} active {{/if}}">
        <div class="icon">
          {{{icons.people}}}
        </div>
        <span>People</span>
      </div>
      <div tabindex="72" class="button settings{{#if page.settings}} active {{/if}}">
        <div class="icon">
          {{{icons.settings}}}
        </div>
        <span>Settings</span>
      </div>
    {{else}}
      <div tabindex="73" class="button you{{#if page.me}} active {{/if}}">
        <div class="icon">
          {{{icons.me}}}
        </div>
        <span>Your work</span>
      </div>
    {{/if}}
    {{#if isDebugMode}}
      <div tabindex="74" class="button debug{{#if page.debug}} active {{/if}}">
        <div class="icon">
          {{{icons.debug}}}
        </div>
        <span>System</span>
      </div>
    {{/if}}
  </div>
</div>